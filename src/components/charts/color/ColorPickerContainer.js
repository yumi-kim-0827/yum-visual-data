import React from "react";
//components
import ColorPicker from "./ColorPicker";

const ColorPickerContainer = ({ handleSelectTheme }) => {
  return <ColorPicker handleSelectTheme={handleSelectTheme} />;
};

export default ColorPickerContainer;
